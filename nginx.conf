server {
    listen 3000;
    root /usr/share/nginx/html;

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SPA: unknown paths fall through to the client router.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
